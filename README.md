opsworks_rails_helpers Cookbook
===============================
Installs and configures some rails helpers on opsworks rails layers.

Before using the commands, don't forget to switch to the root login shell via `sudo su -`.
